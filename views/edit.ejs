<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note Title</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 min-h-screen text-white">

    <!-- NAV -->
    <nav class="max-w-5xl mx-auto px-6 py-6">
        <a href="/" class="text-green-400 hover:underline text-sm">
            ← Back to Home
        </a>
    </nav>

    <section class="md:col-span-1 bg-zinc-800/60 backdrop-blur rounded-xl p-6 shadow-lg">
        <h2 class="text-2xl font-semibold mb-4">Edit Note Title</h2>

        <form action="/edit" method="post" class="space-y-4">

            <!-- ORIGINAL FILENAME (DO NOT EDIT) -->
            <input type="hidden" name="oldFilename" value="<%= filename %>">

            <!-- NEW TITLE -->
            <input type="text" name="newTitle" placeholder="New Title..." value="<%= filename.replace('.txt','') %>"
                class="w-full px-4 py-2 bg-zinc-900 rounded-md outline-none focus:ring-2 focus:ring-green-600" required>

            <button type="submit" class="w-full bg-green-700 hover:bg-green-600 transition py-2 rounded-md font-medium">
                Edit Title
            </button>
        </form>

    </section>

</body>

</html>